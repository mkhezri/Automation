# Copy to .env and fill in values
ANTHROPIC_API_KEY=sk-ant-...

# Optional: SMTP for direct email sending (fallback if Gmail not used)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your-app-password
